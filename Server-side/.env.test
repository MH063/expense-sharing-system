# 测试环境配置（敏感信息通过CI/本地环境注入）
NODE_ENV=test
PORT=3001

# 数据库配置
DB_DIALECT=postgres
DB_STORAGE=
DB_NAME=expense_test

# JWT配置（测试密钥不入库）
JWT_SECRETS=
JWT_REFRESH_SECRETS=
JWT_EXPIRES_IN=24h

# 日志配置
LOG_LEVEL=error