# 接口开发计划

## 阶段六：文档与自动化（进行中）
- 契约测试：已为阶段五新增端点补充 Jest/Supertest 契约测试（disputes/reviews/files/system-config）。
- 错误码：统一标准化错误码响应（`Server-side/middleware/error-handler.js`），字段：`code`、`message`、`timestamp`。
- 文档同步校验：新增脚本 `Server-side/scripts/docs-consistency-check.js`，CI 路径：根目录 `docs:check`；对齐 `server.js` 路由与《接口开发计划.md》端点记载。
- CI脚本：新增 `test:integration` 与 `docs:check` 脚本，可在流水线调用。

### 下一步
- 将 `docs:check` 与 `test:integration` 纳入 CI 流程，设为必过步骤。
- 为管理员、文件、系统配置补充审计日志落库与更多校验规则。
- 输出统一错误码表并在模块控制器内引用。
